version: "3.1"

slots:
  location:
    type: text
  weather_info:
    type: text

responses:
  utter_ask_location:
    - text: "Which location would you like the weather for?"
  utter_weather_info:
    - text: "The current weather in {location} is {weather_info}."

actions:
  - action_check_weather