flows:
  greetings:
    description: greet users when they start a conversation
    persisted_slots:
      - name
    steps:
      - collect: name
        description: the name of the user
      - action: hello_world
  
  provide_capabilities:
    description: explain what the assistant can do and what features are available
    steps:
      - action: utter_provide_info