flows:
  list_menu:
    description: list menu options for users
    name: list menu options
    steps:
      - action: action_check_user_role
        next:
          - if: slots.user_role == "DSNWALI"
            then:
              - id: ask_menu_dosenwali
                collect: list_menu
                utter: utter_ask_list_menu_dosenwali
                ask_before_filling: true
                description: "Meminta user Dosen Wali untuk memilih menu"
                next:
                  - if: slots.list_menu == "Absensi Kelas"
                    then:
                      - link: "absensiKelas"
                  - if: slots.list_menu == "Absensi Ujian"
                    then:
                      - link: "absensiUjian"
                  - if: slots.list_menu == "Bimbingan Diktisaintek Berdampak"
                    then:
                      - link: "bimbinganDiktisaintekBerdampak"
                  - if: slots.list_menu == "Bimbingan Kerja Praktek"
                    then:
                      - link: "bimbinganKerjaPraktek"
                  - if: slots.list_menu == "Bimbingan Skripsi"
                    then:
                      - link: "bimbinganSkripsi"
                  - if: slots.list_menu == "BKD"
                    then:
                      - link: "bkd"
                  - if: slots.list_menu == "Daftar Mahasiswa Per Kelas"
                    then:
                      - link: "daftarMahasiswaPerKelas"
                  - if: slots.list_menu == "Evaluasi Kelas"
                    then:
                      - link: "evaluasiKelas"
                  - if: slots.list_menu == "Melihat Honor"
                    then:
                      - link: "melihatHonor"
                  - if: slots.list_menu == "Membuat Soal Ujian"
                    then:
                      - link: "membuatSoalUjian"
                  - if: slots.list_menu == "Mentoring Dosen"
                    then:
                      - link: "mentoringDosen"
                  - if: slots.list_menu == "Pelaporan PDPT"
                    then:
                      - link: "pelaporanPdpt"
                  - if: slots.list_menu == "Pengisian Nilai"
                    then:
                      - link: "pengisianNilai"
                  - if: slots.list_menu == "Portofolio"
                    then:
                      - link: "portofolio"
                  - if: slots.list_menu == "RPS"
                    then:
                      - link: "rps"
                  - if: slots.list_menu == "Bimbingan Akademik"
                    then:
                      - link: "bimbinganAkademik"
                  - if: slots.list_menu == "Biodata Mahasiswa"
                    then:
                      - link: "biodataMahasiswa"
                  - if: slots.list_menu == "Persetujuan Pengambilan KRS"
                    then:
                      - link: "persetujuanPengambilanKrs"
                  - if: slots.list_menu == "Persetujuan Pengisian KRS"
                    then:
                      - link: "persetujuanPengisianKrs"
                  - else:
                      - action: utter_answer_error_list_menu
                        next: ask_menu_dosenwali

          - if: slots.user_role == "DSN"
            then:
              - id: ask_menu_dosen
                collect: list_menu
                utter: utter_ask_list_menu_dosen
                ask_before_filling: true
                description: "Meminta user Dosen untuk memilih menu"
                next:
                  - if: slots.list_menu == "Absensi Kelas"
                    then:
                      - link: "absensiKelas"
                  - if: slots.list_menu == "Absensi Ujian"
                    then:
                      - link: "absensiUjian"
                  - if: slots.list_menu == "Bimbingan Diktisaintek Berdampak"
                    then:
                      - link: "bimbinganDiktisaintekBerdampak"
                  - if: slots.list_menu == "Bimbingan Kerja Praktek"
                    then:
                      - link: "bimbinganKerjaPraktek"
                  - if: slots.list_menu == "Bimbingan Skripsi"
                    then:
                      - link: "bimbinganSkripsi"
                  - if: slots.list_menu == "BKD"
                    then:
                      - link: "bkd"
                  - if: slots.list_menu == "Daftar Mahasiswa Per Kelas"
                    then:
                      - link: "daftarMahasiswaPerKelas"
                  - if: slots.list_menu == "Evaluasi Kelas"
                    then:
                      - link: "evaluasiKelas"
                  - if: slots.list_menu == "Melihat Honor"
                    then:
                      - link: "melihatHonor"
                  - if: slots.list_menu == "Membuat Soal Ujian"
                    then:
                      - link: "membuatSoalUjian"
                  - if: slots.list_menu == "Mentoring Dosen"
                    then:
                      - link: "mentoringDosen"
                  - if: slots.list_menu == "Pelaporan PDPT"
                    then:
                      - link: "pelaporanPdpt"
                  - if: slots.list_menu == "Pengisian Nilai"
                    then:
                      - link: "pengisianNilai"
                  - if: slots.list_menu == "Portofolio"
                    then:
                      - link: "portofolio"
                  - if: slots.list_menu == "RPS"
                    then:
                      - link: "rps"
                  - else:
                      - action: utter_answer_error_list_menu
                        next: ask_menu_dosen
                    
          - else:
              - id: ask_menu_mahasiswa
                collect: list_menu
                utter: utter_ask_list_menu_mahasiswa
                ask_before_filling: true
                description: "Meminta user Mahasiswa untuk memilih menu"
                next:
                  - if: slots.list_menu == "MB Outbound Non PT"
                    then:
                      - link: "MBoutboundNonPT"
                  - if: slots.list_menu == "TA Skripsi"
                    then:
                      - link: "TAskripsi"
                  - if: slots.list_menu == "Cuti"
                    then:
                      - link: "cuti"
                  - if: slots.list_menu == "Fast Track App"
                    then:
                      - link: "fastTrackApp"
                  - if: slots.list_menu == "Graduation Process"
                    then:
                      - link: "graduationProcess"
                  - if: slots.list_menu == "Kartu Hasil Studi"
                    then:
                      - link: "kartuHasilStudi"
                  - if: slots.list_menu == "Kartu Peserta Ujian"
                    then:
                      - link: "kartuPesertaUjian"
                  - if: slots.list_menu == "Kartu Rencana Studi"
                    then:
                      - link: "kartuRencanaStudi"
                  - if: slots.list_menu == "Konseling"
                    then:
                      - link: "konseling"
                  - if: slots.list_menu == "Konversi"
                    then:
                      - link: "konversi"
                  - if: slots.list_menu == "Majoring Application"
                    then:
                      - link: "majoringApplication"
                  - if: slots.list_menu == "Pembayaran"
                    then:
                      - link: "pembayaran"
                  - if: slots.list_menu == "Pertukaran Mahasiswa"
                    then:
                      - link: "pertukaranMahasiswa"
                  - if: slots.list_menu == "SKPI"
                    then:
                      - link: "skpi"
                  - if: slots.list_menu == "SKPM FK"
                    then:
                      - link: "skpmFK"
                  - if: slots.list_menu == "Transkrip"
                    then:
                      - link: "transkrip"
                  - if: slots.list_menu == "Tugas Akhir"
                    then:
                      - link: "tugasAkhir"
                  - if: slots.list_menu == "Wisuda"
                    then:
                      - link: "wisuda"
                  - else:
                      - action: utter_answer_error_list_menu
                        next: ask_menu_mahasiswa
